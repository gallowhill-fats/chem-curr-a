(assert '(HasClassName "acid class*" "acids"))
(assert '(HasGenericMemberName "acid class*" "acid"))

(assert '(Ako "strong acid class*" "acid class*"))

(assert '(HasClassName "strong acid class*" "strong acids"))
(assert '(HasGenericMemberName "strong acid class*" "strong acid"))

(assert '(HasClassName "base class*" "bases"))
(assert '(HasGenericMemberName "base class*" "base"))

(assert '(Ako "strong base class*" "base class*"))

(assert '(HasClassName "strong base class*" "strong bases"))
(assert '(HasGenericMemberName "strong base class*" "strong base"))

(assert '(HasClassName "buffer solution class*" "buffer solutions"))
(assert '(HasClassName "buffer solution class*" "buffers"))
(assert '(HasGenericMemberName "buffer solution class*" "buffer solutions"))
(assert '(HasGenericMemberName "buffer solution class*" "buffer"))
